{
    "request": {
        "method": "GET",
        "url": "/autobook/vehicles",
	    "headers": {
		    "X-MockUser" : {
			    "equalTo" : "plemire@nuance.com"
		    }
	    }
    },
    "response": {
	    "proxyBaseUrl": "${WINDOWS_GATEWAY_IP}:${WIREMOCK_FORWARD_PORT}"
	}

}
